# AGENTS.md

## 지침

- `.env` 파일을 읽거나 출력하거나 파싱할 수 없다.
- 데이터베이스에 접근할 때는 오직 읽기 작업만 수행할 수 있다.
- 데이터베이스 생성, 수정, 삭제, 마이그레이션, 스키마 변경, `truncate`, `drop` 등 읽기 외의 작업은 수행할 수 없다.

## Python 실행

- 이 프로젝트에서 Python을 실행할 때는 프로젝트 내부 가상환경인 `.\.venv\Scripts\python.exe`를 우선 사용한다.
- 전역 `python` 또는 사용자 AppData 경로의 Python 실행 파일은 Codex 샌드박스에서 접근이 제한될 수 있으므로 직접 사용하지 않는다.
